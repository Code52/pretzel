<Window x:Class="Pretzel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="pretzel.ico"
        Title="Pretzel Express" Height="350" Width="525" ResizeMode="NoResize">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <ListBox ItemsSource="{Binding Sites}" Margin="10,10,10,65">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Directory}" />
                        <TextBlock><Run>http://localhost:</Run><Run FontWeight="Bold" Text="{Binding Port}"/></TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding TogglePause}">></Button>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="New Site" Height="50" Width="100" Click="ButtonBase_OnClick" Margin="10,259,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
    </Grid>
</Window>
